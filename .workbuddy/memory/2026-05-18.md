# 2026-05-18

## ScenReq 前端修复

- 修复 `Req/server/static/index.html` 语言切换时 combo 下拉选项未联动为英文的问题
- 方案：前端维护 `comboTranslations` 中英文映射，`displayCombo()` 方法根据 `lang` 决定显示文本，option 的 `:value` 仍保持中文 combo 标识符传给后端
- 改动位置：setup() 内新增映射和方法，option 模板改为 `displayCombo(c)`，return 中暴露 `displayCombo`
