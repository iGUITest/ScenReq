# 2026-05-07 工作日志

## 实验需求规格说明书编写完成

- **任务**: 根据 ScenReq V2 系统实现 + PDF 实验方案，整理完善的需求说明文档
- **输出文件**: `实验需求规格说明书.md`
- **文档规模**: 约 600 行，11 个章节
- **核心内容**:
  1. 系统架构概述（V2 分层架构、与 V1 差异）
  2. 核心数据模型规范（FunctionIntent / Widget / Screen / ValidationReport，含完整字段表和 JSON 样例）
  3. 5 种输出格式规范（自然语言 / 测试用例 / 用户故事 / Gherkin / 结构化 JSON，均附样例）
  4. 实验设计：5 组精化方式对比（基线 / Skill辅助 / 人工初级 / 人工资深 / EARA闭环）
  5. 20 个 APP 清单（10 开源 + 10 商用，预留版本/需求数/备注字段）
  6. 根因分类体系（R/A/E 三级分类，含关键词规则）
  7. EARA 终止条件与收敛判断
  8. 评估指标体系（准确性 / 效率 / 可验证性 / EARA 专用）
  9. 交接检查清单（代码 / 数据 / 文档）
- **数据来源**:
  - ScenReq V2 源码（`src/models/`, `src/pipeline/`, `src/validation/`, `src/output_generation/`）
  - PDF 实验方案（`不同需求精化方式测试效果对比实验方案.pdf`）
  - ScenReq V1 输出样例（`com.ferrarid.converterpro_SRS.json`）

## 实验执行方案重写完成（第二版）

- **反馈**: 用户对第一版不满意，认为夹杂过多系统架构与数据模型内容，与"实验计划"无关
- **重写聚焦**: 仅保留实验执行层面，去除所有系统架构、代码模型、接口设计
- **输出文件**: `实验执行方案.md`
- **文档规模**: 约 350 行，10 个章节
- **核心内容**:
  1. 实验目标（4 个验证问题）
  2. 20 个 APP 清单（10 开源 + 10 商用，含包名/场景/需求数）
  3. 5 组实验设计（基线/Skill/初级人工/资深人工/EARA 闭环）
  4. 实验执行流程（Day 1–5 按天排期）
  5. 数据记录格式（4 张核心表格的字段定义与示例）
  6. 根因分类标准（R/A/E 二级分类 + 判定补丁）
  7. 评估指标与统计方法（核心指标 + EARA 专用指标 + 对比维度）
  8. 风险预案与补丁方案（需求层/执行层/精化层/数据层，共 16 项风险 + 补丁）
  9. 最终产出物清单（原始数据/统计分析/分析报告）
  10. 快速检查清单（执行前 10 项确认）

## 需求交付规范文档编写完成（第三版）

- **反馈**: 用户连续两次对输出不满意，真实需求是"给别人看的交付规范"而非实验方案
- **真实需求**: 系统输出什么、专家怎么改 Word、怎么保存回 JSON
- **重写聚焦**: 仅围绕 `manual_edit_sync` 模块 + 实际生成的 4 类文件
- **输出文件**: `需求交付与人工修改规范.md`
- **核心内容**:
  1. 系统输出交付物清单（SRS JSON / Tests JSON / Word 报告 / test_json）
  2. 3 种 JSON 数据结构及字段含义（含是否可修改标注）
  3. Word 报告表格结构及 Word↔JSON 字段映射
  4. Description/Objective 分割规则、Test Data 解析规则
  5. 专家修改操作规范（可修改/不可修改清单）
  6. 两种回写方式（手动同步 API / 自动监听 watcher）
  7. 完整协作流程（6 步，给专家的傻瓜式步骤）
  8. 注意事项与常见问题排查
- **数据来源**:
  - `manual_edit_sync/README.md` + `word_json_sync.py` + `api.py`
  - 实际生成的 `*_SRS.json`、`*_TestRequirements.json`、`test_json/*.json`
  - 实际 Word 报告表格结构
