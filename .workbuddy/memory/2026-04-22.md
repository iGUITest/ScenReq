# 2026-04-22 项目日志

## Part 1: 项目清理

### 已完成的操作

| # | 操作 | 详情 |
|---|------|------|
| 1 | 删除 `backend/` | 与 `Req/server/` 完全重复的目录 |
| 2 | 删除 `Req/tools/update/output/decompiled/` | 与 `Req/output/` 重复的反编译数据 |
| 3 | 重构 `parse_flow.py` | 改为增强版兼容层，内部调用 `run_smali_enhancement.py`，保持原有接口不变 |
| 4 | 迁移 `test_smali_enhancement.py` | 修改 `apktool` 引用路径 |
| 5 | 迁移 `GetTestChain_NoFill.py` | 修改 `extract_activities` 引用路径 |
| 6 | 删除原始基础工具 | `extract_activities.py`、`understand_activity.py`、`merge_activity.py` |
| 7 | 更新 `__init__.py` | 添加模块结构文档 |

### 当前 `Req/tools/` 结构
```
Req/tools/
├── __init__.py              ← 模块文档
├── apktool.py               ← 保留：反编译工具
├── parse_flow.py            ← 重构：增强版兼容层
├── report_generator.py      ← 保留：报告生成
├── zip_utils.py             ← 保留：ZIP打包
└── update/                  ← 增强版本
    ├── smali_ir_builder.py
    ├── resource_indexer.py
    ├── intent_graph_builder.py
    ├── llm_assist.py
    ├── run_smali_enhancement.py
    ├── legacy_bridge.py
    ├── test_smali_enhancement.py
    └── 文档/
```

---

## Part 2: ScenReq V2 创建（基于PropGen优化方案）

### 项目结构
```
ScenReq_V2/
├── src/
│   ├── models/              # 数据模型层
│   │   ├── function_intent.py   # FunctionIntent (What/Why/Where/How)
│   │   ├── widget.py            # Widget/Screen 控件模型
│   │   └── app_input.py         # AppInput 输入封装
│   ├── widget_detection/    # 控件检测模块
│   │   └── detector.py          # XML布局解析 + 控件标注
│   ├── intent_generation/   # 功能意图生成
│   │   └── generator.py         # LLM-based 意图推断
│   ├── grounding/           # 控件接地验证
│   │   └── validator.py         # 接地验证 + 修复策略
│   ├── memory_pool/         # 功能记忆池
│   │   └── pool.py              # 签名索引 + 相似度检索
│   ├── validation/          # 需求验证与精化
│   │   └── validator.py         # 多维度验证 + 迭代精化
│   ├── output_generation/   # 多格式输出
│   │   └── generator.py         # NL/用户故事/Gherkin/测试用例
│   └── pipeline/            # 端到端流水线
│       └── pipeline.py          # 整合所有模块
├── tests/                   # 单元测试（全部通过）
├── data/memory_pool/        # 记忆池存储
├── main.py                  # CLI 入口
└── README.md
```

### 核心模块实现状态

| Phase | 模块 | 文件 | 状态 |
|-------|------|------|------|
| Phase 1 | FunctionIntent 数据模型 | `src/models/function_intent.py` | ✅ |
| Phase 1 | Widget/Screen 模型 | `src/models/widget.py` | ✅ |
| Phase 1 | AppInput 输入封装 | `src/models/app_input.py` | ✅ |
| Phase 1 | 控件检测器 | `src/widget_detection/detector.py` | ✅ |
| Phase 1 | 功能记忆池 | `src/memory_pool/pool.py` | ✅ |
| Phase 2 | 功能意图生成器 | `src/intent_generation/generator.py` | ✅ |
| Phase 2 | 控件接地验证器 | `src/grounding/validator.py` | ✅ |
| Phase 3 | 需求验证器 | `src/validation/validator.py` | ✅ |
| Phase 3 | 意图精化器 | `src/validation/validator.py` | ✅ |
| Phase 4 | 多格式输出生成器 | `src/output_generation/generator.py` | ✅ |
| Phase 5 | 端到端流水线 | `src/pipeline/pipeline.py` | ✅ |
| Phase 5 | CLI 入口 | `main.py` | ✅ |

### 测试覆盖

| 测试文件 | 测试内容 | 结果 |
|---------|---------|------|
| `tests/test_models.py` | FunctionIntent序列化、Widget/Screen操作 | ✅ 通过 |
| `tests/test_memory_pool.py` | 添加、检索、保存、加载 | ✅ 通过 |
| `tests/test_validation.py` | 验证器、精化器 | ✅ 通过 |
| `tests/test_output.py` | 5种输出格式 | ✅ 通过 |

### 核心特性实现

| 特性 | 实现状态 |
|------|---------|
| **功能意图层** (What+Why+Where+How) | ✅ 完整实现 |
| **控件接地** | ✅ 验证+修复策略 |
| **功能记忆池** | ✅ 签名索引+Jaccard相似度检索 |
| **迭代精化** | ✅ 最多3轮迭代，4维度验证 |
| **多格式输出** | ✅ NL/用户故事/Gherkin/测试用例/JSON |
| **端到端流水线** | ✅ 完整整合 |

### 使用方法

```bash
# 处理 APK
python main.py --apk ./app.apk --app-name "MyApp" --app-desc "一个笔记应用"

# 处理已反编译目录
python main.py --decompiled ./app_dir --app-name "MyApp"

# 指定输出格式
python main.py --apk ./app.apk --format test_case
```

## 完成工作

### 1. 删除重复目录
- ✅ 删除 `backend/`（与 `Req/server/` 完全重复）
- ✅ 删除 `Req/tools/update/output/decompiled/`（与 `Req/output/` 重复的反编译数据）

### 2. 重构 parse_flow.py 为增强版兼容层
- ✅ 将 `parse_flow.py` 重构为调用增强版流水线 `run_smali_enhancement.py`
- ✅ 保持原有接口不变（`preprocess_apk`, `preprocess_existing_dir`, `analyze_existing_dir`, `analyze_batch_dir`）
- ✅ 内部集成 `legacy_bridge` 导出旧版兼容格式
- ✅ 失败时自动回退到基础处理

### 3. 迁移外部引用
- ✅ `Req/tools/update/test_smali_enhancement.py`：修改 `Req.tools.apktool` → `..apktool`
- ✅ `Req/experiment/GetTestChain_NoFill.py`：修改 `Req.tools.extract_activities` → `Req.tools.parse_flow._extract_activities_from_manifest`
- ✅ `Req/server/main.py`：无需修改，通过 `parse_flow.py` 兼容层自动适配

### 4. 删除原始基础工具
- ✅ 删除 `Req/tools/extract_activities.py`
- ✅ 删除 `Req/tools/understand_activity.py`
- ✅ 删除 `Req/tools/merge_activity.py`

### 5. 保留文件
- `Req/tools/apktool.py` — 反编译工具，增强版测试仍依赖
- `Req/tools/report_generator.py` — Word报告生成，无增强替代
- `Req/tools/zip_utils.py` — ZIP打包，通用工具

### 6. 更新文档
- ✅ 更新 `Req/tools/__init__.py` 添加模块说明

## 当前 tools/ 结构
```
Req/tools/
├── __init__.py              ← 模块文档
├── apktool.py               ← 保留：反编译工具
├── parse_flow.py            ← 重构：增强版兼容层
├── report_generator.py      ← 保留：报告生成
├── zip_utils.py             ← 保留：ZIP打包
└── update/                  ← 增强版本
    ├── smali_ir_builder.py
    ├── resource_indexer.py
    ├── intent_graph_builder.py
    ├── llm_assist.py
    ├── run_smali_enhancement.py
    ├── legacy_bridge.py
    ├── test_smali_enhancement.py
    └── 文档/
```
